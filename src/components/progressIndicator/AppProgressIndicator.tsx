import React from 'react';
import {ActivityIndicator} from 'react-native';

const AppProgressIndicator = () => {
  return <ActivityIndicator size="large" color="#002138" />;
};

export default AppProgressIndicator;
